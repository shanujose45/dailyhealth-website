import React from 'react';
import ReactDOM from 'react-dom/client';
import DailyHealthWebsite from './DailyHealthWebsite';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <DailyHealthWebsite />
  </React.StrictMode>
);
